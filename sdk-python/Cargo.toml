[package]
name = "pace-python"
version = "1.0.0"
edition = "2021"

[lib]
name = "pace_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20.2", features = ["extension-module"] }
pace-engine = { path = "../pace-engine" }